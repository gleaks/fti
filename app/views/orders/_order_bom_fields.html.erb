<div class="row nested-fields">
  <div class="form-group col-sm-6">
    <%= f.label :product_id %>
    <%= f.grouped_collection_select :product_id, @productcategories, :products, :name, :id, :name, {}, { :class=>'form-control s2' } %>
  </div>
  <div class="form-group col-sm-4">
    <%= f.label :count %>
    <%= f.text_field :count, class: 'form-control' %>
  </div>
  <div class="form-group col-sm-2">
    <br>
    <i class="material-icons">clear</i><%= link_to_remove_association "Remove product", f %>
  </div>
</div>
